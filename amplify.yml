version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pip3 install -r requirements.txt
    build:
      commands:
        - make html
  artifacts:
    baseDirectory: _build/html
    files:
      - '**/*'
  cache:
    paths: []
